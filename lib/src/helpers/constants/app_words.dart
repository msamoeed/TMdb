import 'package:flutter/foundation.dart';

/// A utility class that holds constants for words.
/// This class has no constructor and all variables are `static`.
@immutable
class AppWords {
  const AppWords._();

  static const String appName = 'TMdb';
}
